Make sure you deploy the backend file and connect it to a postgres sql (railway works well)
and connect those two by adding variable in the backend service. It should say something like
trying to connect a sql? so press that choose database url and yeah backend is now active.
Next is the frontend you have to go to git pages choose DOCS and save it shud give u a url 
click that sign up and yeah bye.
